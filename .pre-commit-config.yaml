repos:
  - repo: builtin
    hooks:
      - id: check-added-large-files
        exclude: ^backend/tests/fixtures/docling_validation/.*\.pdf$
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: mixed-line-ending
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-executables-have-shebangs

  - repo: local
    hooks:
      - id: ruff-root
        name: ruff check (root)
        entry: uv run --group dev ruff check paperchat_cli tests
        language: system
        files: ^(paperchat_cli|tests)/.*\.pyi?$
      - id: ruff-format-root
        name: ruff format (root)
        entry: uv run --group dev ruff format --check paperchat_cli tests
        language: system
        files: ^(paperchat_cli|tests)/.*\.pyi?$
      - id: ruff
        name: ruff check
        entry: uv run --project backend ruff check
        language: system
        files: ^backend/.*\.pyi?$
      - id: ruff-format
        name: ruff format
        entry: uv run --project backend ruff format --check
        language: system
        files: ^backend/.*\.pyi?$
